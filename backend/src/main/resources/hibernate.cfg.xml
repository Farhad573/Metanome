<?xml version='1.0' encoding='utf-8'?>
<!--
  This copy mirrors backendwar/src/main/resources/hibernate.cfg.xml so running the
  backend module directly (e.g. BackendDevServer) still finds the configuration.
-->
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">org.hsqldb.jdbcDriver</property>
        <property name="connection.url">jdbc:hsqldb:hsql://localhost:9001/metanomedb</property>
        <property name="shutdown">true</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"/>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.HSQLDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">false</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class="de.metanome.backend.results_db.Algorithm"/>
        <mapping class="de.metanome.backend.results_db.Engine"/>
        <mapping class="de.metanome.backend.results_db.DatabaseConnection"/>
        <mapping class="de.metanome.backend.results_db.Execution"/>
        <mapping class="de.metanome.backend.results_db.FileInput"/>
        <mapping class="de.metanome.backend.results_db.Input"/>
        <mapping class="de.metanome.backend.results_db.Result"/>
        <mapping class="de.metanome.backend.results_db.TableInput"/>
        <mapping class="de.metanome.backend.results_db.ResultType"/>
        <mapping class="de.metanome.backend.results_db.ExecutionSetting"/>

        <mapping class="de.metanome.backend.results_db.DpqlExecution"/>
        <mapping class="de.metanome.backend.results_db.DpqlNormalizedTable"/>
        <mapping class="de.metanome.backend.results_db.DpqlNormCell"/>

    </session-factory>

</hibernate-configuration>
